int removeDuplicates(vector<int> &a, int n) {
	// Write your code here.
	if(a.size() ==0){
		return 0;
	}
	int x = 1;
	for(int i=1; i<n; i++){
		if(a[i] ==a[i-1]){
			continue;
		}

		x++;
	}

	return x;
}
